{
  "name": "apna-sahe-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build && node scripts/copy-dist.mjs",
    "dev": "npm --prefix frontend run dev"
  }
}
